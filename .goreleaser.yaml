# GoReleaser 配置文件 - 用于自动化发布和分发
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/ask/main.go
    binary: ask
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.Version={{.Version}} # 动态注入版本号
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

# Homebrew 配置：会自动推送到你的 shanhai1024/homebrew-ask 仓库
brews:
  - name: ask
    repository:
      owner: shanhai1024
      name: homebrew-ask
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"

    directory: Formula
    homepage: "https://github.com/shanhai1024/aiAsk"
    description: "Your AI Terminal Expert | 你的 AI 终端指令专家"
    license: "MIT"

    # 安装脚本逻辑
    install: |
      bin.install "ask"

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "merge"